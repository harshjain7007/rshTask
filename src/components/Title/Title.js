import React from "react";
import "./Style.css";
const Title = ({ title }) => {
  return (
    <div className="Start_Div">
      <span className="Header">{title}</span>
    </div>
  );
};

export default Title;
